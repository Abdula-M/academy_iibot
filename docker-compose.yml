# ============================================================
# Docker Compose — продакшен-конфигурация AI Telegram Bot
#
# Сервисы:
#   bot      — FastAPI + aiogram (Webhook)
#   postgres — PostgreSQL 15 (основная БД)
#   redis    — Redis 7 (FSM storage для aiogram)
#
# Запуск:
#   docker compose up -d --build
#
# Логи:
#   docker compose logs -f bot
# ============================================================

services:
  # ── Приложение (бот) ───────────────────────────────────────
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-bot-image:v3
    container_name: ai-bot
    restart: unless-stopped
    # env_file: .env
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - USE_WEBHOOK=${USE_WEBHOOK:-false}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - WHATSAPP_SERVICE_URL=${WHATSAPP_SERVICE_URL:-http://whatsapp:3000/send}
      - INSTAGRAM_VERIFY_TOKEN=${INSTAGRAM_VERIFY_TOKEN}
      - INSTAGRAM_ACCESS_TOKEN=${INSTAGRAM_ACCESS_TOKEN}
      - MAX_BOT_TOKEN=${MAX_BOT_TOKEN}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - DEEPSEEK_BASE_URL=${DEEPSEEK_BASE_URL:-https://api.deepseek.com}
      - DEEPSEEK_MODEL=${DEEPSEEK_MODEL:-deepseek-chat}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - DEBUG=${DEBUG:-false}
    ports:
      - "${BOT_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - bot-network
    deploy:
      resources:
        limits:
          memory: 512M                   # Запас для 1 async воркера (кушает ~80MB RAM)
        reservations:
          memory: 128M

  # ── PostgreSQL ─────────────────────────────────────────────
  postgres:
    image: postgres:15-alpine
    container_name: ai-bot-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ai_bot}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Задайте POSTGRES_PASSWORD в .env}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-ai_bot}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - bot-network

  # ── Redis ──────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: ai-bot-redis
    restart: unless-stopped
    command: >
      redis-server
        --maxmemory 128mb
        --maxmemory-policy allkeys-lru
        --appendonly yes
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - bot-network

  # ── WhatsApp Microservice ──────────────────────────────────
  whatsapp:
    build:
      context: .
      dockerfile: whatsapp/microservice/Dockerfile
    image: ai-whatsapp-image:v3
    container_name: ai-bot-whatsapp
    restart: unless-stopped
    # env_file: .env
    environment:
      - WEBHOOK_URL=http://bot:8000/api/whatsapp/webhook
      - CLEAR_SESSION=${CLEAR_SESSION:-false}
      # - DEBUG=puppeteer:*
    ports:
      - "3005:3000"
    volumes:
      - whatsapp_data:/app/.wwebjs_auth
    networks:
      - bot-network
    shm_size: 512mb
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ── Frontend (React Dashboard) ─────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ai-bot-frontend
    restart: unless-stopped
    networks:
      - bot-network
  caddy:
    image: caddy:2-alpine
    container_name: ai-bot-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - bot-network

# ── Volumes ──────────────────────────────────────────────────
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  whatsapp_data:
    driver: local
  caddy_data:
    driver: local
  caddy_config:
    driver: local

# ── Network ──────────────────────────────────────────────────
networks:
  bot-network:
    driver: bridge
